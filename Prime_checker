# This is a test python script.
def prime_checker(number):
    if number < 2:
        check = True
    else:
        check = True
        for i in range(2, number/2+1):
            if number % i == 0:
                check = False
    return check
